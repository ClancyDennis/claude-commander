import Root, {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
} from "./button.svelte";
import IconButton, {
  type IconButtonProps,
  type IconButtonSize,
  type IconButtonVariant,
  iconButtonVariants,
} from "./icon-button.svelte";

export {
  Root,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  Root as Button,
  IconButton,
  type IconButtonProps,
  type IconButtonSize,
  type IconButtonVariant,
  iconButtonVariants,
};
