import Root, {
  type ScrollAreaProps,
  type ScrollbarVisibility,
  type ScrollOrientation,
} from "./scroll-area.svelte";

export {
  Root,
  type ScrollAreaProps,
  type ScrollbarVisibility,
  type ScrollOrientation,
  Root as ScrollArea,
};
